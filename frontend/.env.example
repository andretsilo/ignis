# URL of the ignis FastAPI backend.
# In development (npm run dev) Vite proxies /jobs to localhost:8000
# so this variable is not strictly required locally.
# Set it when running the frontend container or building for production.
VITE_API_URL=http://localhost:8000
